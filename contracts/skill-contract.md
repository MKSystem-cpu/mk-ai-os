# Skill Contract v2.0

Every active skill must define:
- mission and responsibility boundary,
- invocation aliases,
- required inputs,
- deterministic execution workflow,
- outputs,
- quality gate,
- handoff fields,
- truthfulness and external-action limits.

A skill is not considered active when its `SKILL.md` contains only generic placeholder steps.
